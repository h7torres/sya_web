# Gallery

One file per image (or a single indexed array file — decide once real
content exists), matching `imageShape` from `../schema.js`.

`collection: null` means the image is standalone. Otherwise it should
match the `id` of a collection in `../collections/`.