# Neighbors

One file per contributor, e.g. `andres-preciado.js`, matching
`neighborShape` from `../schema.js`.

`contributions` is a list of references — not copies — pointing at the
collection or image items that belong to this person. The Neighbors
page resolves these to build links out to Collections/Gallery.