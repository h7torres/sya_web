# Posts

Library-only content — anything site-related that doesn't belong to a
collection, isn't a gallery image, and isn't a neighbor profile. One
file per post, matching `postShape` from `../schema.js`.